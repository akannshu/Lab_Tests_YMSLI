1. In this project we had to provide queries for interaction with database. Also we have to create tables by ourselves.
 whereas Hibernate uses ORM to do such stuff.

2. Creating associations between relations is quite hard in JDBC associations like one-to-one etc can be used using annotations.

3. Hibernate reduced the boiler plate code